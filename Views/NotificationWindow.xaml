<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Gauge.Views.NotificationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootHost" Background="Transparent" Opacity="0"
          Padding="17,9,14,9" ColumnSpacing="14">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" />
        </Grid.RenderTransform>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image x:Name="NotificationIcon" Grid.Column="0"
               Width="30" Height="30" VerticalAlignment="Center" />
        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
            <TextBlock x:Name="TitleText" TextWrapping="Wrap"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" Opacity="0.72"
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>
    </Grid>
</Window>
